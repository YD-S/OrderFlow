# taskfiles/docker.yml
version: '3'

tasks:
  default:
    desc: Start the local stack
    cmds:
      - docker compose up --build

  down:
    desc: Stop the local stack
    cmds:
      - docker compose down